# 🚀 دليل البدء السريع - DataPurity

## ✅ الحالة الحالية

**السيرفرات تعمل الآن!**

- ✅ Backend (FastAPI) - يعمل على `http://localhost:8000`
- ✅ Frontend (React) - يعمل على `http://localhost:5174`

---

## 🎯 ابدأ الآن

### 1️⃣ افتح التطبيق

```
http://localhost:5174/login
```

### 2️⃣ سجل دخول بالحساب التجريبي

```
البريد الإلكتروني: sumayliabdullah@gmail.com
كلمة المرور: password123
```

### 3️⃣ ستنتقل إلى لوحة التحكم

```
http://localhost:5174/dashboard
```

---

## 📱 الصفحات المتاحة

| الصفحة       | الرابط             | الوصف                    |
| ------------ | ------------------ | ------------------------ |
| تسجيل الدخول | `/login`           | الدخول للحساب            |
| حساب جديد    | `/signup`          | إنشاء حساب               |
| لوحة التحكم  | `/dashboard`       | الصفحة الرئيسية          |
| رفع البيانات | `/datasets/upload` | رفع ملفات البيانات       |
| المهام       | `/jobs`            | متابعة المهام الجارية    |
| بطاقات العمل | `/cards/upload`    | رفع ومعالجة بطاقات العمل |
| جهات الاتصال | `/contacts`        | إدارة جهات الاتصال       |
| التصديرات    | `/exports`         | تحميل الملفات المصدرة    |
| الفواتير     | `/billing`         | إدارة الاشتراك           |

---

## 🔧 إدارة السيرفرات

### التحقق من الحالة

```powershell
# التحقق من عمليات Python و Node
Get-Process | Where-Object {$_.ProcessName -like "*python*" -or $_.ProcessName -like "*node*"}
```

### إيقاف السيرفرات

**Frontend:**

- اضغط `Ctrl+C` في نافذة Terminal التي تشغل `npm run dev`

**Backend:**

- اضغط `Ctrl+C` في نافذة Terminal التي تشغل `uvicorn`

### إعادة التشغيل يدوياً

**Backend:**

```powershell
cd D:\datapurity\backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**

```powershell
cd D:\datapurity\frontend
npm run dev
```

---

## 🔐 المصادقة

### كيف تعمل

1. عند تسجيل الدخول، يرسل الـ Frontend طلب إلى:

   ```
   POST http://localhost:8000/api/v1/auth/login
   ```

2. الـ Backend يتحقق من الإيميل وكلمة المرور

3. إذا كانت صحيحة، يرجع JWT Token:

   ```json
   {
     "access_token": "eyJhbGciOiJIUzI1NiIs...",
     "token_type": "bearer"
   }
   ```

4. الـ Frontend يحفظ الـ Token في `localStorage`

5. كل طلب لاحق يرسل الـ Token في الـ Header:

   ```
   Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
   ```

6. الصفحات المحمية تستدعي:
   ```
   GET http://localhost:8000/api/v1/auth/me
   ```

### إنشاء حساب جديد

يمكنك إنشاء حساب جديد من صفحة `/signup` أو عبر API:

```powershell
curl http://localhost:8000/api/v1/auth/signup `
  -X POST `
  -H "Content-Type: application/json" `
  -d '{
    "email": "newuser@example.com",
    "password": "securepassword",
    "full_name": "User Name"
  }'
```

---

## 📊 قاعدة البيانات

### الموقع

```
D:\datapurity\backend\datapurity.db
```

### إعادة إنشاء قاعدة البيانات

```powershell
cd D:\datapurity\backend
python init_db.py
```

هذا سيحذف كل البيانات وينشئ قاعدة جديدة مع المستخدم التجريبي.

---

## 🛠️ API Documentation

### Swagger UI (تفاعلي)

```
http://localhost:8000/api/v1/docs
```

يمكنك تجربة كل الـ endpoints مباشرة من المتصفح!

### ReDoc (وثائق تفصيلية)

```
http://localhost:8000/api/v1/redoc
```

### أمثلة على الاستخدام

**تسجيل دخول:**

```powershell
curl http://localhost:8000/api/v1/auth/login `
  -X POST `
  -H "Content-Type: application/json" `
  -d '{
    "email": "sumayliabdullah@gmail.com",
    "password": "password123"
  }'
```

**الحصول على معلومات المستخدم:**

```powershell
$token = "your-token-here"
curl http://localhost:8000/api/v1/auth/me `
  -H "Authorization: Bearer $token"
```

---

## 🐛 استكشاف الأخطاء

### Frontend لا يفتح

```powershell
# تحقق من أن الـ process يعمل
Get-Process | Where-Object {$_.ProcessName -eq "node"}

# افتح DevTools في المتصفح (F12)
# تحقق من Console و Network tabs
```

### Backend لا يستجيب

```powershell
# تحقق من أن uvicorn يعمل
Get-Process | Where-Object {$_.ProcessName -eq "python"}

# افتح logs في terminal
# ابحث عن أخطاء أو استثناءات
```

### Login يفشل

1. تأكد من البيانات صحيحة:

   - Email: `sumayliabdullah@gmail.com`
   - Password: `password123`

2. افتح DevTools → Network tab

   - تحقق من response لـ `/auth/login`
   - اقرأ رسالة الخطأ

3. تحقق من قاعدة البيانات:
   ```powershell
   cd D:\datapurity\backend
   python init_db.py
   ```

### CORS Errors

إذا ظهرت أخطاء CORS في Console:

1. تأكد من أن Backend يعمل على port `8000`
2. تأكد من أن Frontend يعمل على port `5173` أو `5174`
3. الـ CORS مضبوط للـ ports: `5173`, `5174`, `3000`

---

## 📝 ملاحظات مهمة

### قاعدة البيانات الحالية: SQLite

- مناسبة للتطوير
- **ليست** للإنتاج
- عند النشر، استخدم PostgreSQL

### الميزات المتاحة

✅ **يعمل الآن:**

- تسجيل دخول/حساب جديد
- المصادقة عبر JWT
- الصفحات الأساسية
- واجهة مستخدم تفاعلية

⏳ **قيد التطوير:**

- رفع الملفات
- تنظيف البيانات
- معالجة OCR
- كشف التكرارات
- التصديرات

### الأمان

⚠️ **للتطوير فقط:**

- `DEBUG=True` في `.env`
- `JWT_SECRET` بسيط
- لا يوجد rate limiting
- لا يوجد HTTPS

🔒 **للإنتاج:**

- غير `DEBUG` إلى `False`
- استخدم `JWT_SECRET` معقد
- فعّل rate limiting
- استخدم HTTPS
- استخدم PostgreSQL

---

## 🎉 كل شيء جاهز!

**التطبيق يعمل الآن ومفتوح على:**

👉 **http://localhost:5174/login**

**سجل دخول واستمتع!** 🚀

---

آخر تحديث: 20 نوفمبر 2025
