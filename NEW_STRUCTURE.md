# 🎯 DataPurity - الهيكل الجديد المحسّن

## ✅ ما تم تغييره

### قبل (Frontend منفصل + Backend منفصل):

```
❌ خادمين منفصلين
❌ مشاكل CORS
❌ إعدادات معقدة
❌ صعوبة في النشر
```

### بعد (نظام موحد):

```
✅ خادم واحد فقط في الإنتاج
✅ لا مشاكل CORS
✅ أسرع وأخف
✅ سهل النشر
```

---

## 🚀 كيفية التشغيل

### 1️⃣ وضع التطوير (Development)

**الأفضل أثناء التطوير - يوفر Hot Reload**

```powershell
.\dev.ps1
```

**سيفتح نافذتين:**

- Frontend Dev Server: `http://localhost:5173` (مع Hot Reload)
- Backend API: `http://localhost:8000` (مع Auto-reload)

**استخدم هذا الوضع عندما:**

- تريد تعديل الكود
- تحتاج رؤية التغييرات فوراً
- تختبر ميزات جديدة

---

### 2️⃣ وضع الإنتاج (Production)

**الأفضل للاستخدام النهائي والنشر**

```powershell
.\build-and-run.ps1
```

**سيحدث التالي:**

1. بناء Frontend إلى ملفات ثابتة
2. تشغيل Backend الذي يخدم Frontend أيضاً
3. التطبيق الكامل على: `http://localhost:8000`

**استخدم هذا الوضع عندما:**

- تريد اختبار النسخة النهائية
- تريد نشر التطبيق
- تريد أداء أفضل

---

## 🏗️ الهيكل الجديد

```
datapurity/
│
├── backend/
│   ├── app/
│   │   ├── main.py          ← يخدم API + Frontend معاً
│   │   ├── routers/         ← API endpoints
│   │   └── core/            ← الإعدادات
│   └── requirements.txt
│
├── frontend/
│   ├── src/                 ← كود React
│   ├── dist/                ← بعد البناء (يُخدم من Backend)
│   └── package.json
│
├── dev.ps1                  ← تشغيل التطوير
├── build-and-run.ps1        ← بناء + تشغيل الإنتاج
└── README.md
```

---

## 🔄 كيف يعمل؟

### في وضع التطوير:

```
Frontend (Vite:5173)  ←→  Backend (FastAPI:8000)
      ↓                          ↓
  Hot Reload              Auto-reload
```

### في وضع الإنتاج:

```
المستخدم → Backend:8000
              ├── /api/v1/* → API endpoints
              └── /* → ملفات Frontend الثابتة
```

---

## 📊 المقارنة

| الميزة              | قبل   | بعد         |
| ------------------- | ----- | ----------- |
| عدد الخوادم (إنتاج) | 2     | 1           |
| مشاكل CORS          | ✗ نعم | ✓ لا        |
| سرعة التحميل        | عادية | ✓ أسرع      |
| سهولة النشر         | صعبة  | ✓ سهلة جداً |
| Hot Reload (تطوير)  | ✓ نعم | ✓ نعم       |

---

## 🛠️ الأوامر المفيدة

### إعادة بناء Frontend فقط

```powershell
cd frontend
npm run build
```

### تشغيل Backend فقط

```powershell
cd backend
d:\datapurity\.venv\Scripts\uvicorn.exe app.main:app --reload
```

### إيقاف جميع الخوادم

```powershell
Get-Process -Name python,node -ErrorAction SilentlyContinue | Stop-Process -Force
```

### فتح API Docs

```
http://localhost:8000/api/v1/docs
```

---

## 🌐 للنشر على الإنترنت

1. **بناء Frontend:**

```bash
cd frontend
npm install
npm run build
```

2. **رفع على السيرفر:**

```bash
# انسخ المجلد الكامل
scp -r . user@server:/app/datapurity
```

3. **تشغيل على السيرفر:**

```bash
cd /app/datapurity/backend
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

أو باستخدام **Gunicorn** (للإنتاج):

```bash
gunicorn app.main:app \
  -w 4 \
  -k uvicorn.workers.UvicornWorker \
  -b 0.0.0.0:8000
```

---

## 📝 ملاحظات مهمة

✅ **ملفات Frontend** موجودة في `frontend/dist/` بعد البناء  
✅ **Backend** يخدمها تلقائياً من `app/main.py`  
✅ **كل طلب** يبدأ بـ `/api/v1/` يذهب للـ API  
✅ **باقي الطلبات** تُخدم من React (SPA)

---

## 🎉 ملخص سريع

```powershell
# للتطوير (نافذتين - Hot Reload)
.\dev.ps1

# للإنتاج (نافذة واحدة - أسرع)
.\build-and-run.ps1
```

**هذا كل شيء! 🚀**
