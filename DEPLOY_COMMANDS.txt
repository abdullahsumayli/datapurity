═══════════════════════════════════════════════════════════════
📋 أوامر النشر - انسخ والصقها في SSH الخادم
═══════════════════════════════════════════════════════════════

# انسخ كل الأوامر التالية والصقها دفعة واحدة:

cd /opt/datapurity && \
git pull origin main && \
sudo systemctl daemon-reload && \
sudo systemctl restart datapurity-backend && \
cd frontend && \
npm ci && \
npm run build && \
cd .. && \
sudo systemctl restart datapurity-frontend && \
sudo cp /opt/datapurity/infra/nginx.conf /etc/nginx/nginx.conf && \
sudo nginx -t && \
sudo systemctl reload nginx && \
echo "✅ اكتمل النشر!" && \
echo "" && \
echo "📊 حالة الخدمات:" && \
sudo systemctl status datapurity-backend --no-pager -l | head -n 8 && \
echo "" && \
sudo systemctl status datapurity-frontend --no-pager -l | head -n 8

═══════════════════════════════════════════════════════════════
🔍 للتحقق من الأخطاء (إذا لزم):
═══════════════════════════════════════════════════════════════

# سجلات Backend:
sudo journalctl -u datapurity-backend -n 30 --no-pager

# سجلات Frontend:
sudo journalctl -u datapurity-frontend -n 20 --no-pager

# سجلات Nginx:
sudo tail -n 20 /var/log/nginx/error.log

═══════════════════════════════════════════════════════════════
🌐 اختبار الموقع:
═══════════════════════════════════════════════════════════════

curl -I https://aidotoo.com
curl -I https://aidotoo.com/api/v1/health
curl -I https://aidotoo.com/favicon.ico

═══════════════════════════════════════════════════════════════
